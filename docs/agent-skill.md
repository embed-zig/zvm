# Agent Skill

The project-level skill at `skills/zvm/SKILL.md` gives agents the preferred workflow for installing or updating zvm itself, installing Zig toolchains, switching versions, and verifying PATH state.

Keep the skill short and operational. It should not duplicate the full registry format or implementation docs; link agents back to the project docs when details are needed.
